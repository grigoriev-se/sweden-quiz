# Local tooling config (Claude Code's dev-server launcher) — not part of the app
.claude/

# OS / editor cruft
Thumbs.db
desktop.ini
.DS_Store
.vscode/
.idea/

# Personal build journal. Kept local on purpose — it is a private manuscript,
# not project documentation. PLAN.md and README.md are the published ones.
DEVLOG.md

# Python
__pycache__/
*.pyc
.venv/

# Nothing else to ignore yet: no dependencies, no build output.
# TODO: add `node_modules/` and `dist/` if this ever moves to a Node toolchain.
