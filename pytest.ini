[pytest]
# Without this, pytest puts tests/ on the import path but NOT the project root,
# so `from app.main import app` fails. This adds the root.
pythonpath = .
testpaths = tests
addopts = --continue-on-collection-errors
